# Required: Anthropic API key for AI classification
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Custom database path (defaults to backend/parly_monitor.db)
# DATABASE_PATH=/data/parly_monitor.db

# Optional: Host/port for the server (defaults to 127.0.0.1:8000)
# HOST=0.0.0.0
# PORT=8000

# Optional: Max concurrent keyword searches during scan (defaults to 12)
# Higher values = faster scans but more concurrent API calls
# KEYWORD_PARALLELISM=12
